# Prompt Log — v2.4 Wilderness Journal Retarget + v2.5 Quality Eval Mode

**Date**: 2026-06-27
**Stage**: 0
**Agent**: claude-code
**Project**: azure-da-mcp / `agents/` (v2.0 A2A platform) + `adapt-to-2026-demo`
**Status**: Complete ✅
**Topic**: Retarget the v2.0 mesh from the retired `da-live-postal-2025-07` site to the new `adapt-to-2026-demo` Wilderness Journal site, split the IA (`/ai-content` reference vs `/ai-articles` generated), deprecate the legacy folders, and add a `quality` eval mode that scores generated content on its own merits. Shipped `v2.4.0` + `v2.5.0`.

> Mostly an autonomous session: only 3 substantive user messages. A `/goal` Stop hook drove the agent to plan → change → deploy → run → monitor → iterate without pausing. The detail below summarizes the autonomous work under each prompt.

---

## Session: Retarget to the Wilderness Journal demo site

### Prompt 1: The goal — retarget the platform + update docs (2:14 PM)

> OK - we have our demo website built and we're going to move away completely from da postal thing from 2025 07. read `adapt-to-2026-demo/local-only/wilderness-journal-migration-handoff.md` completely and understand our 2.0 thing. Also update our readme that `content-authoring-eval` and `agent-claude-sdk` are all officially deprecated, and release notes and what you[r].
>
> anyways - two things, update documentation and focus on 2.0, and uhmm... make adjustment. so that we're able to do our daily page and as well as content pages fully pointing to https://main--adapt-to-2026-demo--jackzhaojin.aem.page/
>
> make a note that https://main--adapt-to-2026-demo--jackzhaojin.aem.page/ai-content is for like, content that is to be referenced as best practice, and stuff we generated should maybe in a different path, maybe ai-articles or something.
>
> this may require you to do deployments and what not.
>
> _(follow-up, 2:17 PM)_ I should add that you have write access to `adapt-to-2026-demo` - if you see code things we can make better (like the author picture for some...) [go ahead].

→ Response: Mapped the full data flow (coordinator generate/migrate stages had the old postal site hardcoded, no site/folder/blockLibrary awareness); built per-site profiles, wired site→content-gen + folder/reference/pattern→migration, added the IA split, deprecated both legacy folders (doc-only, D5-safe), de-risked the da.live S2S write path end-to-end, deployed, ran the real cloud loop, and tagged `v2.4.0`.
→ Action: New `coordinator/src/site-profiles.ts` (lane `wilderness-journal`, folder `ai-articles`, reference `/ai-content/**`, `pattern: article`); coordinator threads `siteBrief`/`pageType`/`folder`/`blockLibraryUrl`/`neighborPageUrl`/`pattern` + clean slugs; `migration.run.v1` gained `folder`/`neighborPageUrl`/`pattern` + article recipe mimicking `chasing-sunsets`; content-gen wilderness lane + real Unsplash heroes (killed picsum); daily loop defaults to `adapt-to-2026-demo`; deprecation banners in root README/CLAUDE + both subproject READMEs + RELEASES; fixed author-avatar crop (`crop=faces&w=600&h=600`) across the reference corpus via the deployed DA MCP; typecheck + 61 e2e green; commit `a62a9c3`, deploy run `28302140552`, tagged **`v2.4.0`** (`d828104`) + GitHub release. Verified S2S read/write/preview-publish on the new site (the early failures were the Anthropic MCP-proxy timeout + a leading-underscore filename, not auth). ✅

---

### Prompt 2: Keep going (10:26 PM)

> Let's keep going!!

→ Response: Picked up the follow-up flagged at v2.4.0 — the full-loop eval scores source→target *fidelity*, which understates generated-content quality (visual 29 = the redesign succeeding, not failing). Added an eval `mode`: `fidelity` (default) vs `quality`, where quality scores content on its own merits (new intrinsic content-quality scorer) and forces visual intrinsic. Validated locally (67→88 on the live deadfall article), deployed, confirmed on the deployed cloud eval (89), tagged `v2.5.0`, then ran a clean end-to-end finale.
→ Action: `feat(eval): quality mode` (`9086e70`) — new intrinsic content-quality scorer reusing the deterministic `fetchHTML`/`extractWebpageContent`; visual/structure/accessibility already had no-source paths; wired a `mode` field through the eval executor; 61 e2e green; deployed; direct cloud quality eval = **89 (good)**; bumped + tagged **`v2.5.0`** (`b93a38e`) + release. Clean end-to-end loop scored **88 (PASS)** — same article that scored a misleading 67 in fidelity mode. (One run got tangled with the concurrent v2.5.0 tag deploy → migrate zombie from a mid-turn container rollout; cancelled and re-ran clean on the settled mesh.) ✅

---

### Prompt 3: We're good (12:09 AM, 2026-06-28)

> I think we're good!

→ Response: Recapped the record — `v2.4.0` (retarget) + `v2.5.0` (quality eval mode) both live on Cloudflare and green; clean full loop authored a real article into `/ai-articles` scoring 88 (PASS) in quality mode; daily cron (09:20 UTC) now produces a fresh quality-scored Wilderness Journal article each day at `content-factor-dash.jackzhaojin.com`; legacy folders deprecated + IA split documented + state saved to memory.
→ Action: None (wrap-up). Noted two optional future follow-ups: the migration restart-rebuild interruption gap and slug/grammar polish. ✅

---

**Session Summary**: Retargeted the v2.0 mesh to the `adapt-to-2026-demo` Wilderness Journal site via per-site profiles + IA split (`/ai-content` reference vs `/ai-articles` generated), deprecated `content-authoring-eval` + `agent-claude-sdk`, and added a `quality` eval mode. Two clean end-to-end production loops (88 / 88 PASS). Shipped `v2.4.0` + `v2.5.0` to Cloudflare.
