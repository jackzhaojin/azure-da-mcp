{
  "name": "@agents/deploy",
  "private": true,
  "description": "M5: Cloudflare Workers + Containers deployment of the A2A mesh (content-factory*.xpri.ai). Standalone (NOT an npm workspace) — wrangler needs Node 22.",
  "scripts": {
    "sync-skill": "rm -rf skills && mkdir -p skills && cp -R ../../.claude/skills/da-live-author-playwright skills/",
    "predeploy": "npm run sync-skill",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail content-factory"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  }
}
