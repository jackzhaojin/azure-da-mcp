{
  "name": "@agents/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "E2E tests for the agent mesh \u2014 real servers, real A2A protocol over HTTP, no mocks (monorepo testing philosophy)",
  "scripts": {
    "test": "vitest run",
    "test:live": "vitest run --config vitest.live.config.ts"
  },
  "devDependencies": {
    "@a2a-js/sdk": "0.3.13",
    "better-sqlite3": "^12.4.1",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
